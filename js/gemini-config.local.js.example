/**
 * Optional LOCAL override (gitignored as gemini-config.local.js).
 *
 * Prefer Cloudflare Worker for live site (see worker/README.md).
 * Use this only for local experiments without a worker.
 */
window.GEMINI_CONFIG = {
  enabled: true,
  mode: "direct",
  apiKey: "PASTE_YOUR_KEY_HERE",
  model: "gemini-2.0-flash",
};
