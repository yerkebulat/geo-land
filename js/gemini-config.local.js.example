/**
 * Copy this file to: js/gemini-config.local.js
 * (gemini-config.local.js is gitignored — safe to put your key there)
 *
 * Then enable Gemini in the browser after the main config loads.
 */
window.GEMINI_CONFIG = {
  enabled: true,
  apiKey: "PASTE_YOUR_KEY_HERE",
  model: "gemini-2.0-flash",
};
